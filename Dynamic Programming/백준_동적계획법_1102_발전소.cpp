#include<cstdio>

using namespace std;

int n, p;
char st[17];
int ar[17][17];
int ans;

int loop(int idx, int cnt,int cost) {
	if(cnt>)
}

int main() {
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		for (int j = 0; j < n; j++) scanf("%d", ar[i][j]);
	scanf("%s", st);
	scanf("%d", &p);
	printf("%d", ans);
}